var app = angular.module("firstApp", []);
app.controller("MyFirstController", function($scope){
  $scope.name = "Severus Snape";
  $scope.favColor = "blue";
  $scope.secondsInACentury = 60*60*24*365*100;
  // Create a property called rightNow in the controller that 
  // is equal to the current time (use a built-in JS function t
//   o find the time). Display it in the view and format it nicely 
// (using a built-in filter) so that it looks something like 
// this:
})

